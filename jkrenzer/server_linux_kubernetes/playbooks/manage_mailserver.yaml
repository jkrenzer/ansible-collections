---
- name: Manage Mailserver
  become: true
  hosts: mailserver
  environment:
    K8S_AUTH_API_KEY: "{{ token | quote }}"
    K8S_AUTH_KUBECONFIG: /home/{{ ansible_user }}/.kube/config
  roles:
    - jkrenzer.server_linux_kubernetes.mailserver