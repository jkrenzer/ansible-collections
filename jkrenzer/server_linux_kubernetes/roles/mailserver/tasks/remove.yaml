---
- name: Remove chart
  kubernetes.core.helm:
    chart_ref: "{{ mailserver_config.helm.chart_ref }}"
    create_namespace: "{{ mailserver_config.helm.create_namespace }}"
    release_name: "{{ mailserver_config.helm.release_name }}"
    release_namespace: "{{ mailserver_config.helm.release_namespace }}"
    release_state: absent
    update_repo_cache: true

- name: Remove the repo
  kubernetes.core.helm_repository:
    name: "{{ mailserver_config.helm.repo_name }}"
    repo_url: "{{ mailserver_config.helm.repo_url }}"
    repo_state: absent
